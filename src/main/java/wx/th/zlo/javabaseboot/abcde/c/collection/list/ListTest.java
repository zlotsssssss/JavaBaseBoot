package wx.th.zlo.javabaseboot.abcde.c.collection.list;


// List保证插入顺序排序，元素可重复，通过索引直接操作元素
/*
新增方法：
get(index)
add(index,e)
set(index,e)
remove(index)
等和索引有关的方法
sort( Comparator<? extends E>) 用Arrays.sort


 */
public interface ListTest {
    public String s= "1";
}
